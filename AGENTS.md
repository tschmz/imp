# AGENTS.md

- Commit messages must follow the Conventional Commits format, for example `feat: add status endpoint`.
- Before every commit, run `npm run check` and `npm test`, and do not commit unless both pass.

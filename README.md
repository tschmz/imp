# imp

`imp` is a lightweight local daemon for modular agent bots.

The project is currently focused on a small, robust core:
- local installation via npm
- JSON-based configuration
- Telegram DM transport
- bot-scoped runtime data and process management

`imp` is still in an early stage and may change quickly.
